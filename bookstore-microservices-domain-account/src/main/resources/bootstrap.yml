spring:
  application:
    name: account
  cloud:
    # config:
    #   uri: http://${CONFIG_HOST:localhost}:${CONFIG_PORT:8888}
    #   fail-fast: true
    #   password: ${CONFIG_PASS:dev}
    #   username: user
    nacos:
      config:
        server-addr: ${CONFIG_HOST:localhost}:${CONFIG_PORT:8848}
        file-extension: yaml
      discovery:
        server-addr: ${CONFIG_HOST:localhost}:${CONFIG_PORT:8848}
